alert("Mensagem!"); // A função alert() não está no node. Só funciona no navegador. 

window.confirm("Deseja cancelar?"); // assim como o alert();, o confirm(); é do navegador. Eles são objetos do window.

window.prompt("Digite seu nome: ");

let numero1 = prompt("Digite um número: ");